from distutils.core import setup

setup(
      name = "subpy",
      license = "BSD",
      description = "Python subsets",
      packages = ['subpy',
                  'subpy.tests',],
      version = "0.1",
)
